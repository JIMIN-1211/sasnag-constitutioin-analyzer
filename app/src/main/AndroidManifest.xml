<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true" >

        <!-- 앱 실행 시 제일 먼저 실행되는 화면 = Splash -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 스플래시 이후 이동할 화면 -->
        <activity android:name=".ui.AgreementActivity"/>
        <activity android:name=".ui.InfoActivity"/>
        <activity android:name=".ui.EmailActivity"/>
        <activity android:name=".ui.MainActivity"/>
        <activity android:name=".ui.BodyInfoActivity"/>
        <activity android:name=".ui.ResultActivity"/>
        <activity android:name=".ui.IdLoginActivity"/>
        <activity android:name=".ui.RecordSimpleActivity"/>
        <activity android:name=".ui.QuestionsSingleActivity"/>
        <activity android:name=".ui.HealthRecordActivity"/>
        <activity android:name=".ui.MealRecordActivity"/>
        <activity android:name=".ui.MyPageActivity"/>
        <activity android:name=".ui.WorkoutRecordActivity" />
        <activity android:name=".ui.RecordSimpleActivity" />
    </application>

</manifest>